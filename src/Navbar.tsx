import React from 'react';
import { Link } from 'react-router-dom';

const Navbar: React.FC = () => {
  return (
    <nav>
      <ul>
        {/* Remove Home and Projects links from here */}
      </ul>
    </nav>
  );
}

export default Navbar;
